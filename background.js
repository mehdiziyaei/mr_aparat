// chrome.runtime.onMessage.addListener(
  // function(request, sender, sendResponse) {
    // if (request.message == "StartMA")
	// {
		// chrome.runtime.sendMessage({message: "StartMrAparat"});
		
	// }
	// if (request.message == "StopMA")
	// {
	
		// chrome.runtime.sendMessage({message: "StopMrAparat"});
		      
	// }
  // });
  
 